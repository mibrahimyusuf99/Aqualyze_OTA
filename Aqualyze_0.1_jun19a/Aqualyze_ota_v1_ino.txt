/*
 ============================================================
 PROGRAM: Firmware V1 - OTA Bootstrap
 FILE   : Aqualyze_ota_v1.ino
 ENTITAS: ESP32 DevKit 38-pin
 THING  : Aqualyze_OTA_Node
 DEVICE : Aqualyze_0.1
 VERSI  : 1.0.0
 UPLOAD : Pertama kali melalui USB
 ============================================================
*/
#include "thingProperties.h"

#define LED_PIN 2   // pin led

const char* FIRMWARE_VERSION = "1.0.0";

unsigned long lastCounterMillis = 0;
const unsigned long counterInterval = 10000; // 10 detik

void setup() {
  Serial.begin(115200);
  delay(1500);

  pinMode(LED_PIN, OUTPUT);
  digitalWrite(LED_PIN, LOW);

  initProperties();
  ArduinoCloud.begin(ArduinoIoTPreferredConnection);
  setDebugMessageLevel(2);
  ArduinoCloud.printDebugInfo();

  firmwareVersionCloud = FIRMWARE_VERSION;
  deviceStatus = "V1 Online";
  Serial.println("Firmware V1 - boot selesai");
}

void loop() {
  ArduinoCloud.update();
  updateCounterV1();
}

void updateCounterV1() {
  if (millis() - lastCounterMillis >= counterInterval) {
    otaCounter++;
    lastCounterMillis = millis();
    deviceStatus = "V1 Online - tick " + String(otaCounter);
    Serial.print("V1 tick: ");
    Serial.println(otaCounter);
  }
}

void onLedStateChange() {
  digitalWrite(LED_PIN, ledState ? HIGH : LOW);
}
/*
  Since DeviceStatus is READ_WRITE variable, onDeviceStatusChange() is
  executed every time a new value is received from IoT Cloud.
*/
void onDeviceStatusChange()  {
  // Add your code here to act upon DeviceStatus change
}
/*
  Since FirmwareVersionCloud is READ_WRITE variable, onFirmwareVersionCloudChange() is
  executed every time a new value is received from IoT Cloud.
*/
void onFirmwareVersionCloudChange()  {
  // Add your code here to act upon FirmwareVersionCloud change
}
/*
  Since OtaCounter is READ_WRITE variable, onOtaCounterChange() is
  executed every time a new value is received from IoT Cloud.
*/
void onOtaCounterChange()  {
  // Add your code here to act upon OtaCounter change
}
/*
  Since Ledstate is READ_WRITE variable, onLedstateChange() is
  executed every time a new value is received from IoT Cloud.
*/
void onLedstateChange()  {
  // Add your code here to act upon Ledstate change
}